#pragma once
/**
 * @brief función que impreme toda la informacion de cada archivo dentro de un zip.
 * @param ruta_archivo_zip un string conteniendo la ruta completa o relativa de un archivo zip.
 * @returns void
*/ 
void get_data(const char* ruta_archivo_zip);